# Compact CPU run — not the IEEE TAI manuscript

JSON in this folder was produced by `scripts/run_pipeline.py --config configs/compact.yaml` on a CPU host.

It is a methodology smoke test. It is **not** Tables X–XXII of the paper.

Paper-of-record numbers live in `results/paper/manuscript_record.json`.
